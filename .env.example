# Frontend env — copy to `.env.local` (gitignored) and fill in.
# Values also set in Netlify (see the Netlify deploy ticket).
VITE_SUPABASE_URL=https://api.notifier.yoshevbot.uk
VITE_SUPABASE_ANON_KEY=
# base64url application-server (VAPID) public key; wired in the web-push slice.
VITE_VAPID_PUBLIC_KEY=
